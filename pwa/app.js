const $=s=>document.querySelector(s),config=globalThis.LAN_SHARE_PWA_CONFIG||{};let state=JSON.parse(localStorage.getItem("lanSharePwa")||"{}");let selected=null;const auth=()=>({authorization:`Bearer ${state.token||""}`});const saveState=()=>localStorage.setItem("lanSharePwa",JSON.stringify(state));function setJoin(){ $("#join").hidden=false;$("#peers").hidden=true;$("#files").hidden=true;$("#room").value=state.room||"";$("#token").value=state.token||"";$("#discovery").value=state.discovery||config.discoveryUrl||""}async function discover(){if(!state.room||!state.token||!state.discovery)return setJoin();const r=await fetch(`${state.discovery.replace(/\/$/,"")}/v1/rooms/${encodeURIComponent(state.room)}/peers`,{headers:auth()});if(!r.ok)throw Error("PC一覧を取得できませんでした。");const {peers}=await r.json();$("#join").hidden=true;$("#peers").hidden=false;const list=$("#peer-list");list.textContent="";for(const peer of peers){const b=$("#peer-template").content.firstElementChild.cloneNode(true);b.textContent=peer.name;b.onclick=()=>openPeer(peer);list.append(b)}if(!peers.length)list.textContent="起動中のPCを探しています…"}async function openPeer(peer){selected=peer;$("#files").hidden=false;$("#peer-name").textContent=peer.name;const r=await fetch(`${peer.url}/api/files`,{headers:auth()});if(!r.ok)throw Error("PCへ接続できませんでした。LAN接続と権限を確認してください。");const data=await r.json();const list=$("#file-list");list.textContent="";for(const file of data.files){const row=$("#file-template").content.firstElementChild.cloneNode(true);row.querySelector("h3").textContent=file.name;row.querySelector("p").textContent=`${file.sizeLabel} ・ ${new Date(file.modifiedAt).toLocaleString("ja-JP")}${file.protected?" ・ 🔒 保護済み":""}`;row.querySelector(".download").onclick=()=>download(file);list.append(row)}}async function download(file){let token="";if(file.protected){const password=prompt("パスワードを入力してください");if(password===null)return;const r=await fetch(`${selected.url}/api/files/${encodeURIComponent(file.name)}/unlock`,{method:"POST",headers:{...auth(),"content-type":"application/json"},body:JSON.stringify({password})});if(!r.ok)return alert("パスワードが違うか、解除できませんでした。");token=(await r.json()).token}location.href=`${selected.url}/files/${encodeURIComponent(file.name)}${token?`?token=${encodeURIComponent(token)}`:""}`}$("#save").onclick=()=>{state={room:$("#room").value.trim(),token:$("#token").value.trim(),discovery:$("#discovery").value.trim()};saveState();discover().catch(e=>alert(e.message))};$("#settings").onclick=setJoin;$("#upload").onchange=async e=>{if(!selected)return;for(const file of e.target.files){const password=confirm(`${file.name}にパスワードを設定しますか？`)?prompt("パスワード")||"":"";const r=await fetch(`${selected.url}/api/upload`,{method:"POST",headers:{...auth(),"content-type":"application/octet-stream","x-file-name":encodeURIComponent(file.name),"x-file-password":encodeURIComponent(password)},body:file});if(!r.ok)alert(`${file.name}をアップロードできませんでした。`)}openPeer(selected)};let deferred;addEventListener("beforeinstallprompt",e=>{e.preventDefault();deferred=e;$("#install").hidden=false});$("#install").onclick=()=>deferred?.prompt();if("serviceWorker"in navigator)navigator.serviceWorker.register("/sw.js");const query=new URLSearchParams(location.search);if(query.has("room")){state={room:query.get("room"),token:query.get("token"),discovery:query.get("discovery")};saveState();history.replaceState(null,"",location.pathname)}discover().catch(e=>alert(e.message));setInterval(()=>discover().catch(()=>{}),5000);
